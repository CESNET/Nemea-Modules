bin_SCRIPTS=amplification2idea.py
TESTS=test.sh

confdir=${sysconfdir}/warden/amplificationdetector
conf_DATA=config.cfg

supdir=${nemeasupdir}/reporters
sup_DATA=amplification2idea.sup

EXTRA_DIST=${bin_SCRIPTS} ${TESTS} ${conf_DATA} ${sup_DATA}

clean-local:
	rm -f *.idea

