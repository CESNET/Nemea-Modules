bin_SCRIPTS=bruteforce2idea.py
TESTS=test.sh

confdir=${sysconfdir}/warden/bruteforce
conf_DATA=config.cfg

supdir=${nemeasupdir}/reporters
sup_DATA=bruteforce2idea.sup

EXTRA_DIST=${bin_SCRIPTS} ${TESTS} ${conf_DATA} ${sup_DATA}

clean-local:
	rm -f *.idea

