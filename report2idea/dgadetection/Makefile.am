bin_SCRIPTS=dgadetection2idea.py
TESTS=
confdir=${sysconfdir}/warden/dgadetection
conf_DATA=

supdir=${nemeasupdir}/reporters
sup_DATA=dgadetection2idea.sup

EXTRA_DIST=${bin_SCRIPTS} ${TESTS} ${conf_DATA} ${sup_DATA}

clean-local:
		rm -f *.idea
