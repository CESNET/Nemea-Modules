bin_SCRIPTS=venom2idea.py
TESTS=test.sh

confdir=${sysconfdir}/warden/venom
conf_DATA=config.cfg

supdir=${nemeasupdir}/reporters
sup_DATA=venom2idea.sup

EXTRA_DIST=${bin_SCRIPTS} ${TESTS} ${conf_DATA} ${sup_DATA}

clean-local:
	rm -f *.idea

