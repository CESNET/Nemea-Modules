bin_SCRIPTS=sipbf2idea.py
TESTS=test.sh

confdir=${sysconfdir}/warden/sipbruteforce
conf_DATA=config.cfg

supdir=${nemeasupdir}/reporters
sup_DATA=sipbf2idea.sup

EXTRA_DIST=${bin_SCRIPTS} ${TESTS} ${conf_DATA} ${sup_DATA}

clean-local:
	rm -f *.idea

